import React from 'react';

export function ListOfDecrement(props) {
    // let useState;
    // let [listOfDecrement] = useState([]);
    return (
        <div>
            {/* {props.listOfDecrement.map((number, i) => {
                return <ul key={i}>{number}</ul>
            })
            } */}
            {/* <ListOfDecrement
                listOfDecrement={listOfDecrement} /> */}
        </div>
    )
}